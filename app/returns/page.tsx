export const metadata = {
  title: "Returns"
};

export default function ReturnsPage() {
  return (
    <main className="bg-page pt-28 text-copy">
      <section className="container-luxe max-w-4xl py-14 md:py-14">
        <p className="mb-5 text-xs font-bold uppercase tracking-[0] text-gold">Returns</p>
        <h1 className="font-display text-4xl md:text-5xl">A refined exchange policy.</h1>
        <p className="mt-8 text-sm leading-7 text-copy-muted">
          Size exchanges are supported within 7 days of delivery for unworn garments with original tags and packaging.
          Refunds are reserved for confirmed manufacturing faults.
        </p>
      </section>
    </main>
  );
}
